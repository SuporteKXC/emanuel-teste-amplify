export interface ResponsibleData {
  id: number;
  name: string;
  import_slug: string;
  created_at: string;
}

export interface ResponsibleProductData {
  product_id: number;
  responsible_id: number;
}