version: "3.8"

services:
  app:
    build:
      context: ..
      dockerfile: iff/Dockerfile
    container_name: iff_comex_import_api
    ports:
      - "3333:3333"
    environment:
      - NODE_ENV=production
